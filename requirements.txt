fasttext==0.9.3
# fasttext 0.9.3 (latest published version) calls np.array(..., copy=False),
# which raises under numpy>=2.0's stricter copy semantics.
numpy<2
stanza
tksheet
# Runtime dependencies for the Phase 5F reranker infrastructure (reranking.py, formerly reranker_integration.py).
joblib
scipy
scikit-learn